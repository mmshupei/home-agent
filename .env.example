# Required
ANTHROPIC_API_KEY=sk-ant-...
AUTH_HMAC_KEY=                     # 64 hex chars; generate with `python -c "import secrets; print(secrets.token_hex(32))"`

# M1: optional (these come into play in later milestones)
HA_TOKEN=
PUSHOVER_APP_TOKEN=
PUSHOVER_USER_KEY_SHUPEI=
PUSHOVER_USER_KEY_YUNYAN=

# Paths
AGENT_DB_PATH=./data/agent.db
AGENT_RUNS_DIR=./data/runs
